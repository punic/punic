filter:
  paths:
  - src/
  excluded_paths:
  - src/data/

checks:
  php:
    code_rating: true
    duplication: true

tools:
  external_code_coverage: true
  php_code_sniffer: true
  php_mess_detector: true
